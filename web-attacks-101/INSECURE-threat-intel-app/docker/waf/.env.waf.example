# WA101 WAF lab toggles — copy to .env in the docker/ directory, or export in your shell.
#
#   DetectionOnly  Log attacks but let them through (Lab Mode A)
#   On             Block matching attacks with HTTP 403 (Lab Mode B)
#   Off            Disable the WAF entirely (baseline comparison)
MODSEC_RULE_ENGINE=DetectionOnly
